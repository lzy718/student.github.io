个人项目实战：二维码签到系统 后端端开发总结
一、个人职责说明
在本次二维码签到系统团队开发中，我负责后端所有开发工作，包括技术选型、数据库设计、工具类封装、核心业务接口开发、数据校验、安全逻辑实现与接口联调。独立完成整套后端服务，为前端页面提供稳定、安全、可调用的接口支撑，实现二维码动态生成、时效控制、签到校验、数据持久化全套核心能力。



二、后端技术选型
结合项目轻量化、高安全性、易部署的校园考勤场景，选用稳定成熟的后端技术栈：
- 核心框架：SpringBoot 2.7.x，快速构建Web接口，简化配置
- 二维码处理：Google ZXing 开源工具，实现离线二维码生成
- 数据库：MySQL 存储活动与签到数据
- 持久层框架：MyBatis-Plus，简化CRUD开发
- 辅助工具：Lombok、Hutool，简化代码与日期、令牌处理
- 视图模板：Thymeleaf，支持后端跳转前端页面


三、数据库结构设计
为适配签到业务，我设计两张核心数据表，并通过联合唯一索引从数据库底层杜绝重复签到，保障数据严谨性。
3.1 签到活动表
用于存储每一次签到活动信息、唯一签到令牌与二维码过期时间。
CREATE TABLE `sign_activity` (
  `id` bigint AUTO_INCREMENT COMMENT '活动ID',
  `activity_name` varchar(100) NOT NULL COMMENT '活动名称',
  `sign_token` varchar(64) NOT NULL COMMENT '签到唯一令牌',
  `expire_time` datetime NOT NULL COMMENT '二维码过期时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_token` (`sign_token`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='签到活动表';
3.2 签到记录表
记录用户签到数据，设置活动ID+用户ID联合唯一索引，防止重复打卡。
CREATE TABLE `sign_record` (
  `id` bigint AUTO_INCREMENT,
  `activity_id` bigint NOT NULL COMMENT '活动ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `sign_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '签到时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_activity_user` (`activity_id`,`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='签到记录表';



四、核心工具类开发
我基于ZXing封装二维码工具类，无需生成本地文件，直接将图片流响应给浏览器，适配前端动态渲染，同时优化编码、容错率，避免扫码失败、乱码问题。
import com.google.zxing.BarcodeFormat;
import com.google.zxing.EncodeHintType;
import com.google.zxing.client.j2se.MatrixToImageWriter;
import com.google.zxing.qrcode.QRCodeWriter;
import com.google.zxing.qrcode.decoder.ErrorCorrectionLevel;
import org.springframework.stereotype.Component;

import javax.servlet.http.HttpServletResponse;
import java.io.OutputStream;
import java.util.HashMap;
import java.util.Map;

@Component
public class QRCodeUtil {
    public void createQrCode(String content, int width, int height, HttpServletResponse response) throws Exception {
        response.setContentType("image/png");
        Map<EncodeHintType, Object> hints = new HashMap<>();
        hints.put(EncodeHintType.ERROR_CORRECTION, ErrorCorrectionLevel.H);
        hints.put(EncodeHintType.CHARACTER_SET, "UTF-8");
        hints.put(EncodeHintType.MARGIN, 1);

        QRCodeWriter writer = new QRCodeWriter();
        var matrix = writer.encode(content, BarcodeFormat.QR_CODE, width, height, hints);
        try (OutputStream os = response.getOutputStream()) {
            MatrixToImageWriter.writeToStream(matrix, "PNG", os);
        }
    }
}



五、实体类开发
根据数据表结构编写对应实体类，使用Lombok简化代码，实现字段与数据库精准映射。
5.1 活动实体
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("sign_activity")
public class SignActivity {
    private Long id;
    private String activityName;
    private String signToken;
    private LocalDateTime expireTime;
    private LocalDateTime createTime;
}
5.2 签到记录实体
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("sign_record")
public class SignRecord {
    private Long id;
    private Long activityId;
    private Long userId;
    private LocalDateTime signTime;
}



六、核心接口开发与业务逻辑
我完成全部后端接口开发，包含页面跳转、动态二维码生成、签到三重校验核心逻辑，解决代签、过期、重复签到三大痛点。
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;
import javax.annotation.Resource;
import javax.servlet.http.HttpServletResponse;
import java.time.LocalDateTime;
import java.util.UUID;

@Controller
@RequestMapping("/sign")
public class SignController {

    @Resource
    private QRCodeUtil qrCodeUtil;
    @Resource
    private SignActivityService activityService;
    @Resource
    private SignRecordService recordService;

    // 跳转前端签到页面
    @GetMapping("/index")
    public String toSignPage(){
        return "signIndex";
    }

    // 生成5分钟时效动态二维码
    @GetMapping("/create")
    public void createQr(Long activityId, HttpServletResponse response) throws Exception {
        String token = UUID.randomUUID().toString().replace("-", "");
        LocalDateTime expire = LocalDateTime.now().plusMinutes(5);
        SignActivity activity = new SignActivity();
        activity.setId(activityId);
        activity.setSignToken(token);
        activity.setExpireTime(expire);
        activityService.updateById(activity);
        String url = "http://localhost:8080/sign/check?token=" + token + "&activityId=" + activityId;
        qrCodeUtil.createQrCode(url, 300, 300, response);
    }

    // 签到校验核心接口
    @ResponseBody
    @GetMapping("/check")
    public String doSign(String token, Long activityId, Long userId) {
        // 1. 校验二维码合法性
        SignActivity activity = activityService.getById(activityId);
        if (activity == null || !activity.getSignToken().equals(token)) {
            return "签到二维码无效！";
        }
        // 2. 校验二维码是否过期
        if (LocalDateTime.now().isAfter(activity.getExpireTime())) {
            return "二维码已过期，请重新生成！";
        }
        // 3. 防重复签到校验
        LambdaQueryWrapper<SignRecord> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(SignRecord::getActivityId, activityId).eq(SignRecord::getUserId, userId);
        if (recordService.count(wrapper) > 0) {
            return "你已完成本次签到，请勿重复打卡！";
        }
        // 写入签到数据
        SignRecord record = new SignRecord();
        record.setActivityId(activityId);
        record.setUserId(userId);
        record.setSignTime(LocalDateTime.now());
        recordService.save(record);
        return "签到成功！";
    }
}



七、后端核心难点与解决方案
7.1 解决截图代签漏洞
传统固定二维码可无限截图代签，我通过动态UUID令牌 + 5分钟时效机制，每次刷新二维码都会更新全新令牌，超时自动失效，从根源杜绝代签。
7.2 解决重复签到问题
采用双层防护：代码层查询拦截重复请求，数据库联合唯一索引兜底，彻底解决高并发下重复数据插入问题。
7.3 前后端联调适配
适配前端页面渲染规则，二维码以图片流形式输出，添加时间戳防缓存机制，保证前端刷新即可更新最新二维码，完美配合前端交互逻辑。
八、个人开发总结
本次项目我完成后端全套开发工作，从数据库设计、工具封装、业务接口开发到安全逻辑优化、前后端联调全部落地。通过本次实战，我熟练掌握了SpringBoot接口开发、ZXing二维码开发、数据库索引优化、接口安全校验等核心技术，深刻理解了时效业务、防重业务的开发思路，提升了问题排查与后端服务优化能力，也熟悉了前后端协同开发的流程与规范。