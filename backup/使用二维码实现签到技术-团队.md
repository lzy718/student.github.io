Java 实现二维码签到系统｜前后端完整版实战教程（含源码+核心原理）
一、项目概述
针对传统人工点名效率低、易代签、统计繁琐的痛点，本文基于 SpringBoot + ZXing + 原生前端页面 实现完整版二维码签到系统。系统包含前端展示页面 + 后端核心接口，支持动态时效二维码展示、在线刷新二维码、扫码签到、提示反馈、防重复打卡、数据持久化，前后端完整可独立运行。

二、系统核心流程与亮点
2.1 业务流程
1. 后端生成唯一UUID令牌，绑定签到活动并设置5分钟过期时间；
2. 前端页面请求后端接口，动态渲染展示签到二维码；
3. 用户手机扫描前端展示的二维码，携带参数发起签到请求；
4. 后端完成二维码合法性、时效、重复签到三重校验；
5. 校验通过写入数据库，前端弹窗展示签到结果。
2.2 核心优势
- 前后端完整架构，页面可视化操作，告别纯接口测试
- 动态时效二维码，支持手动刷新，杜绝截图代签
- 代码+数据库双层防重复签到，数据安全可靠
- 轻量化开发，无需Vue/React，原生HTML+JS即可运行
- 离线可用，不依赖第三方接口，部署简单
- 
三、全套技术栈
3.1 后端技术栈
- 核心框架：SpringBoot 2.7.x
- 二维码工具：Google ZXing
- 数据库：MySQL
- 持久层：MyBatis-Plus
- 辅助工具：Lombok、Hutool
- 运行环境：JDK 1.8+
3.2 前端技术栈
- 页面架构：原生 HTML5 + CSS3
- 交互逻辑：原生 JavaScript
- 样式适配：Bootstrap 轻量化样式
- 网络请求：AJAX 异步请求后端接口

四、核心Maven依赖
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>2.7.18</version>
</parent>

<dependencies>
    <!-- Web核心 -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- 前端模板引擎（支持静态页面） -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>

    <!-- ZXing二维码工具 -->
    <dependency>
        <groupId>com.google.zxing</groupId>
        <artifactId>core</artifactId>
        <version>3.5.1</version>
    </dependency>
    <dependency>
        <groupId>com.google.zxing</groupId>
        <artifactId>javase</artifactId>
        <version>3.5.1</version>
    </dependency>

    <!-- MyBatis-Plus -->
    <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-boot-starter</artifactId>
        <version>3.5.3.1</version>
    </dependency>

    <!-- MySQL驱动 -->
    <dependency>
        <groupId>mysql</groupId>
        <artifactId>mysql-connector-java</artifactId>
        <scope>runtime</scope>
    </dependency>

    <!-- 工具类 -->
    <dependency>
        <groupId>cn.hutool</groupId>
        <artifactId>hutool-all</artifactId>
        <version>5.8.22</version>
    </dependency>
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>
</dependencies>

五、数据库设计
系统核心包含两张数据表，通过联合唯一索引实现数据库层防重，保障数据唯一性。
5.1 签到活动表
CREATE TABLE `sign_activity` (
  `id` bigint AUTO_INCREMENT COMMENT '活动ID',
  `activity_name` varchar(100) NOT NULL COMMENT '活动名称',
  `sign_token` varchar(64) NOT NULL COMMENT '签到唯一令牌',
  `expire_time` datetime NOT NULL COMMENT '二维码过期时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_token` (`sign_token`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='签到活动表';
5.2 签到记录表
CREATE TABLE `sign_record` (
  `id` bigint AUTO_INCREMENT,
  `activity_id` bigint NOT NULL COMMENT '活动ID',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `sign_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '签到时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_activity_user` (`activity_id`,`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='签到记录表';

六、后端核心代码
6.1 二维码工具类
基于ZXing封装工具类，直接输出图片流至浏览器，适配前端页面渲染。
import com.google.zxing.BarcodeFormat;
import com.google.zxing.EncodeHintType;
import com.google.zxing.client.j2se.MatrixToImageWriter;
import com.google.zxing.qrcode.QRCodeWriter;
import com.google.zxing.qrcode.decoder.ErrorCorrectionLevel;
import org.springframework.stereotype.Component;

import javax.servlet.http.HttpServletResponse;
import java.io.OutputStream;
import java.util.HashMap;
import java.util.Map;

@Component
public class QRCodeUtil {
    public void createQrCode(String content, int width, int height, HttpServletResponse response) throws Exception {
        response.setContentType("image/png");
        Map<EncodeHintType, Object> hints = new HashMap<>();
        hints.put(EncodeHintType.ERROR_CORRECTION, ErrorCorrectionLevel.H);
        hints.put(EncodeHintType.CHARACTER_SET, "UTF-8");
        hints.put(EncodeHintType.MARGIN, 1);

        QRCodeWriter writer = new QRCodeWriter();
        var matrix = writer.encode(content, BarcodeFormat.QR_CODE, width, height, hints);
        try (OutputStream os = response.getOutputStream()) {
            MatrixToImageWriter.writeToStream(matrix, "PNG", os);
        }
    }
}
6.2 实体类代码
活动实体类
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("sign_activity")
public class SignActivity {
    private Long id;
    private String activityName;
    private String signToken;
    private LocalDateTime expireTime;
    private LocalDateTime createTime;
}
签到记录实体类
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import java.time.LocalDateTime;

@Data
@TableName("sign_record")
public class SignRecord {
    private Long id;
    private Long activityId;
    private Long userId;
    private LocalDateTime signTime;
}
6.3 后端控制器（新增页面跳转接口）
新增页面跳转接口，同时保留二维码生成、签到校验核心逻辑，适配前端联动。
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;
import javax.annotation.Resource;
import javax.servlet.http.HttpServletResponse;
import java.time.LocalDateTime;
import java.util.UUID;

@Controller
@RequestMapping("/sign")
public class SignController {

    @Resource
    private QRCodeUtil qrCodeUtil;
    @Resource
    private SignActivityService activityService;
    @Resource
    private SignRecordService recordService;

    // 跳转前端签到页面
    @GetMapping("/index")
    public String toSignPage(){
        return "signIndex";
    }

    // 生成5分钟时效签到二维码
    @GetMapping("/create")
    public void createQr(Long activityId, HttpServletResponse response) throws Exception {
        String token = UUID.randomUUID().toString().replace("-", "");
        LocalDateTime expire = LocalDateTime.now().plusMinutes(5);
        SignActivity activity = new SignActivity();
        activity.setId(activityId);
        activity.setSignToken(token);
        activity.setExpireTime(expire);
        activityService.updateById(activity);
        String url = "http://localhost:8080/sign/check?token=" + token + "&activityId=" + activityId;
        qrCodeUtil.createQrCode(url, 300, 300, response);
    }

    // 扫码签到校验接口
    @ResponseBody
    @GetMapping("/check")
    public String doSign(String token, Long activityId, Long userId) {
        SignActivity activity = activityService.getById(activityId);
        if (activity == null || !activity.getSignToken().equals(token)) {
            return "签到二维码无效！";
        }
        if (LocalDateTime.now().isAfter(activity.getExpireTime())) {
            return "二维码已过期，请重新生成！";
        }
        LambdaQueryWrapper<SignRecord> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(SignRecord::getActivityId, activityId).eq(SignRecord::getUserId, userId);
        if (recordService.count(wrapper) > 0) {
            return "你已完成本次签到，请勿重复打卡！";
        }
        SignRecord record = new SignRecord();
        record.setActivityId(activityId);
        record.setUserId(userId);
        record.setSignTime(LocalDateTime.now());
        recordService.save(record);
        return "签到成功！";
    }
}

七、前端页面完整代码
在 resources/templates 下创建 signIndex.html 签到主页面，实现二维码展示、刷新功能、签到提示，页面简洁美观、适配电脑端展示。
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>二维码签到系统</title>
    <style>
        *{margin: 0;padding: 0;box-sizing: border-box;}
        .container{width: 400px;margin: 80px auto;text-align: center;}
        .title{font-size: 24px;font-weight: bold;margin-bottom: 30px;color: #333;}
        .qrcode-box{border: 1px solid #eee;padding: 20px;border-radius: 8px;}
        #qrcode{width: 300px;height: 300px;margin: 0 auto;}
        .tip{color: #666;margin: 15px 0;}
        .refresh-btn{padding: 10px 30px;background: #409eff;color: #fff;border: none;border-radius: 4px;cursor: pointer;}
        .refresh-btn:hover{background: #66b1ff;}
    </style>
</head>
<body>
    <div class="container">
        <div class="title">校园二维码签到系统</div>
        <div class="qrcode-box">
            <div id="qrcode"></div>
            <p class="tip">扫码即可签到（二维码5分钟有效）</p>
            <button class="refresh-btn" onclick="refreshQr()">刷新二维码</button>
        </div>
    </div>

<script>
    // 初始化加载二维码
    window.onload = function(){
        refreshQr();
    }

    // 刷新二维码
    function refreshQr(){
        // 时间戳防止缓存
        let time = new Date().getTime();
        document.getElementById("qrcode").innerHTML = `<img src="/sign/create?activityId=1&t=${time}" width="300" height="300">`;
    }
</script>
</body>
</html>
八、前后端联调核心机制
- 页面访问逻辑：用户访问 /sign/index，后端跳转前端签到主页；
- 二维码渲染逻辑：前端通过img标签请求后端二维码生成接口，动态渲染图片；
- 防缓存机制：刷新二维码携带时间戳，避免浏览器缓存导致二维码不更新；
- 签到交互逻辑：手机扫码自动请求后端校验接口，返回文字提示结果。
九、核心安全机制解析
- 防代签机制：每次刷新二维码生成全新UUID令牌，5分钟自动过期，杜绝截图代签。
- 防重机制：代码层查询拦截+数据库联合唯一索引兜底，彻底避免重复签到。
- 安全机制：动态带Token签到链接，无法批量伪造，安全性远超固定二维码。
十、完整项目测试流程
1. 向sign_activity表插入一条测试活动数据；
2. 启动项目，访问 http://localhost:8080/sign/index 进入前端签到页面；
3. 页面自动加载二维码，可手动点击刷新更新时效二维码；
4. 手机扫码，拼接 &userId=用户ID 完成签到，查看返回结果；
5. 重复访问触发防重拦截，超时访问触发过期拦截，功能完全正常。
十一、优化与拓展方向
- Redis缓存Token，降低数据库压力，提升并发性能；
- 新增GPS定位校验，限制指定范围内签到，杜绝远程代签；
- 优化前端，增加用户登录、签到记录查询页面；
- 添加Excel签到名单导出、管理员权限控制功能。
十二、总结
本文基于SpringBoot+ZXing+原生前端页面，实现了前后端完整的二维码签到系统。项目包含可视化前端操作页面、后端核心校验逻辑、数据库数据持久化，具备时效校验、防伪造、防重复签到等工业级能力，告别纯接口测试，项目完整性拉满，部署简单、适配校园考勤场景。